version: 2

seeds:
  - name: people
    description: "Region-to-regional manager mapping table"
    config:
      schema: raw
      column_types:
        region: string
        regional_manager: string
    columns:
      - name: region
        tests:
          - unique
          - not_null
      - name: regional_manager